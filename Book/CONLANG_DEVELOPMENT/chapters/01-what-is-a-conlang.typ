#import "../design.typ": *

#chapter(number: 1, title: "What is a constructed language?")

A *constructed language* is a language that someone designed on purpose, rather
than one that grew up naturally among a community of speakers over centuries.
English, Mandarin, and Swahili are *natural languages* — nobody invented them;
they evolved. Quenya (spoken by the Elves in Tolkien's books), Klingon (from
*Star Trek*), and Dothraki (from *Game of Thrones*) are constructed languages —
each was built, word by word and rule by rule, by a person.

#term("Constructed language (conlang)")[
  A language deliberately created by one or more people, with an invented
  vocabulary and grammar, instead of arising naturally. The activity of making
  one is called *conlanging*, and a person who does it is a *conlanger*.
]

#section("Why make one?")

People build conlangs for many reasons, and yours is as good as any:

#set enum(numbering: "1.")
+ *Storytelling.* A believable invented language makes a fictional world feel
  real. A few words of a character's native tongue carry more weight than pages
  of description.
+ *Worldbuilding.* Place names, character names, and inscriptions all flow from
  a language. A consistent one makes a map or a history hang together.
+ *Art and play.* A language is an enormous, beautiful puzzle. Many people build
  them purely for the joy of the craft.
+ *Understanding.* Building a language teaches you, from the inside, how real
  languages solve the problem of turning thoughts into sound.

#section("The parts of a language")

Every human language, natural or constructed, has the same broad layers. This
book has one part for each, and they build on one another in order:

#term("Phonology")[
  The sound system of a language: which sounds it uses and how they may be
  combined. This is the foundation — you choose it first, because everything
  else is spoken (or written) using these sounds. *Part II.*
]

#term("Lexicon")[
  The vocabulary — the full set of words. A single word in the lexicon is a
  *lexeme*. Building the lexicon means coining words and recording what they
  mean. *Part III.*
]

#term("Grammar")[
  The rules for how words change their shape and combine into phrases and
  sentences. It has two halves: *morphology* (how a single word changes form,
  for example to mark "more than one") and *syntax* (how words are ordered into
  sentences). *Part IV.*
]

#term("Diachronics")[
  The history of a language — how its sounds and words change over time, and how
  one language splits into a family of related daughter languages. Optional, but
  it is what gives a language depth and realism. *Part V.*
]

#term("Writing system (orthography)")[
  How the language is written down: an alphabet, a syllabary, or a set of
  symbols, and the rules for using them. You can borrow the Latin alphabet you
  are reading now, or invent a script of your own. *Part VI.*
]

#section("A priori and a posteriori")

Conlangers use two Latin phrases you may meet elsewhere. An *a priori* language
is built from scratch, owing nothing to any existing language — its words and
grammar are wholly invented. An *a posteriori* language is based on real
languages, like the way Esperanto draws its vocabulary from European tongues.
This book builds an *a priori* language, because it is the clearest way to see
every part of the process. Nothing stops you from leaning on a favourite real
language for inspiration as you go.

#section("Naturalism: should it feel real?")

A common goal is a *naturalistic* conlang — one that could plausibly be a real
human language, with the kinds of patterns and irregularities that natural
languages have. Inkhaven is built to help here: it knows about the typical
shapes of sound systems, the common ways languages mark grammar, and the natural
ways sounds change over time, and it will gently steer you toward choices that
feel real. But naturalism is a choice, not a rule. If you want a perfectly
regular, clockwork language, you can build that too.

#callout(label: "You do not have to do everything")[
  A language does not need a thousand words, a deep history, and a hand-drawn
  alphabet to be useful or satisfying. A few dozen well-chosen words and a
  simple grammar are plenty for naming places and characters in a story. Read
  the parts you need; the book is built so each pillar stands on its own once
  you have the foundations.
]

#recap((
  [A *conlang* is a language built on purpose; the craft is *conlanging*.],
  [Every language has layers: *phonology* (sounds), *lexicon* (words),
   *grammar* (rules), optional *diachronics* (history), and a *writing system*.],
  [We build them in that order, each resting on the last.],
  [*Naturalism* — making the language feel like a real one — is a goal you can
   choose or ignore.],
))
