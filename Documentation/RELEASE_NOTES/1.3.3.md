# Inkhaven 1.3.3 — The Planning Board, fluent & complete (planning)

_On top of 1.3.2 (The Planning Board).  1.3.2 shipped the analyze + pacing
slice; 1.3.3 makes the Board **fluent** (map beats without hand-editing
HJSON) and **complete** (the blank-page / plan-first mode), then sweeps the
deferred loose ends.  Full plan:
[PLANNING-2_PLAN.md](../PROPOSALS/PLANNING-2_PLAN.md)._

## Headline

- **Interactive mapping** — `plan map`/`unmap` write-back + an interactive
  `Ctrl+V Shift+K` outline (map a beat to a chapter from a picker, jump to
  its chapter, cycle status, link threads). Kills the slug-hunting friction.
- **Plan-first scaffolding** — `plan scaffold --premise "<logline>"`
  expands each framework beat into an intention; `--chapters` materializes
  chapter shells from the beats (opt-in, guarded). The blank-page mode.
- **Consolidation** — `actions/checkout@v5`; a `stale-submission` doctor
  scan; the CLI Prompts-book prompt tier; `I`-lift of a streamed draft into
  its system book.

## Landed so far

- **Planning Board — slug discovery.** `inkhaven plan check` now ends with
  a **CHAPTER SLUGS** list (the values to put in a beat's `mapped_chapter`)
  and a **THREAD SLUGS** list (for `threads`), so you can map beats without
  hunting for slugs in the filesystem.  Tutorial 67 expanded with a worked
  mapping example.

## PDF-1 follow-ups

The 1.3.0 print-production pipeline gains four polish passes (Tutorial 65):

- **More imposition profiles** — `us_perfect` / `us_chapbook` (the Tabloid
  / US-Letter analogues of `default` / `chapbook`) and `thick` (8-sheet
  push-out signatures for heavy books). Five built-ins now.
- **Cover front-art fit + spine auto-fit** — the front image is aspect-
  preserved: `--fit cover` (default) fills and centre-crops, `fit` letter-
  boxes, `stretch` is the old distort. Spine text auto-shrinks to the spine
  height / thickness (never above `spine_font_size_pt`) and is dropped when
  the spine is too thin to carry legible type.
- **Grayscale for JPEGs** — `pdf grayscale` now desaturates DCTDecode
  (JPEG) photos too, re-embedding them as grayscale JPEGs (filter kept, no
  Flate bloat); previously left as colour.
- **Preflight press hazards** — overprint (`/OP` states), transparency
  (alpha < 1, non-Normal blend modes, soft masks), and spot colours
  (Separation / DeviceN colorants) are now reported alongside DPI / fonts /
  blank pages.

## Deferred (later cuts)

- **Tension-curve overlay** (Planning P4.2) — needs a tension model
  (expected-tension per beat + a derivation from linked-thread tensions).
- **Scene cards** — per-scene goal / conflict / disaster, a finer grain
  than beats.

## Carried-in polish (small)

- `stale-submission` doctor scan; the CLI Prompts-book prompt tier; `I`-lift
  of a streamed submission/analysis draft into its system book; interactive
  beat→chapter mapping in the structure-outline view.
- CI: `actions/checkout@v4` → v5 (the Node 20 deprecation) across the three
  workflows.

## Carryovers (still open)

- **The Whole-Book AI Editor** (RAG over the whole manuscript) → 1.4+.
- **1.2.20:** ePub inline images + popup footnotes.
- **Facts follow-ups (1.2.21):** internal-consistency check, series-shared
  facts.
- Sixth supported language; GitHub Release backfill for 1.2.11–1.2.19.

## Implementation log

_(entries land here as the work lands)_
